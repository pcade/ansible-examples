all:
  children:
    vagrant:
      hosts:
        demo-vm:
          ansible_host: 192.168.56.10
          ansible_user: "{{ vault_vagrant_user }}"
          ansible_ssh_private_key_file: "{{ vault_vagrant_key }}"
          ansible_ssh_common_args: "-o StrictHostKeyChecking=no"
